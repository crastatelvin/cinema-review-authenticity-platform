export default function FlaggedReviewList() {
  return (
    <section className="panel" style={{ padding: 18 }}>
      <h4 style={{ marginTop: 0 }}>Flagged Review Highlights</h4>
      <p className="muted" style={{ marginBottom: 0 }}>
        Detailed flagged-review extraction is enabled when source-level review records are returned by active scrapers.
      </p>
    </section>
  );
}
